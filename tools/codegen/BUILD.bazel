load("@rules_python//python:defs.bzl", "py_binary")

# py_binary representing a tool we use DURING the build process.
py_binary(
    name = "generator",
    srcs = ["generator.py"],
    visibility = ["//visibility:public"],
)
